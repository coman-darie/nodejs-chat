The following dependencies have to be installed:
npm install --save express@4.10.2
npm install --save socket.io

The chat can be accessed via any browser using http://localhost:3000

In order to open multiple instances of a client chat, just open additional browser tabs.



